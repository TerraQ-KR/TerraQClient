import 'package:flutter/material.dart';

class QuestStatusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quest Status'),
      ),
      body: const Center(
        child: Text('Quest Status'),
      ),
    );
  }
}
