import 'package:flutter/material.dart';

class QuestDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quest Detail'),
      ),
      body: const Center(
        child: Text('Quest Detail'),
      ),
    );
  }
}
