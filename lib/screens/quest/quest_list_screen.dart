import 'package:flutter/material.dart';

class QuestListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quest List'),
      ),
      body: const Center(
        child: Text('Quest List'),
      ),
    );
  }
}
